const Header = ({label}) => {
    return (
        <>
            <div className="header content-center">
                <h1>{label}</h1>
            </div>
        </>
    );
}
 
export default Header;